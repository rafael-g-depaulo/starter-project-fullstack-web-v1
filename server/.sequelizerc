require("@babel/register");
const { resolve } = require("path")

module.exports = {
  "config":           resolve(__dirname, "server/Database/config.js"),
  "migrations-path":  resolve(__dirname, "server/Migrations"),
  "models-path":      resolve(__dirname, "server/Models"),
  "seeders-path":     resolve(__dirname, "server/Seeds"),
}
