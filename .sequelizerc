const { resolve } = require("path")

module.exports = {
  "config":           resolve(__dirname, "server/database/config.js"),
  "migrations-path":  resolve(__dirname, "server/database/migrations"),
  "models-path":      resolve(__dirname, "server/database/models"),
  "seeders-path":     resolve(__dirname, "server/database/seeds"),
}